# Lightweight dependencies for Lambda function
# Heavy dependencies (numpy, boto3, etc.) are provided by Lambda layer

# Caching
cachetools>=5.3.0

# Document processing (essential only)
PyPDF2>=3.0.1
python-docx>=1.1.0
Pillow>=10.1.0

# Text processing (minimal)
textstat>=0.7.3

# JSON handling (faster than standard json)
orjson>=3.9.0

# Security
cryptography>=41.0.0

# Configuration
python-dotenv>=1.0.0
